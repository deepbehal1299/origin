{
  "name": "origin",
  "private": true,
  "description": "Origin Coffee Aggregator — PWA for discovering and comparing coffees from Indian roasters",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "backend": "npm run -w backend start",
    "frontend": "npm run -w frontend dev"
  },
  "engines": {
    "node": ">=18"
  }
}
